import { Coffee, X } from 'lucide-react'; import type { Conversation } from '../types/chat';
export function ConversationHistory({items,onSelect,onClose}:{items:Conversation[];onSelect:(x:Conversation)=>void;onClose:()=>void}){return <aside className="history"><header><b>Conversation History</b><button onClick={onClose} aria-label="Close history"><X size={18}/></button></header><p>Today</p>{items.length===0?<small>No previous conversations this session.</small>:items.map(x=>{const first=x.messages.find(m=>m.role==='user')?.content ?? 'New conversation';return <button className="history-item" key={x.id} onClick={()=>onSelect(x)}><Coffee size={15}/><span><b>{first.slice(0,30)}</b><small>{first}</small></span></button>})}</aside>}
