import { Send } from 'lucide-react'; import { useRef } from 'react';
export function ChatInput({onSend,loading}:{onSend:(value:string)=>void;loading:boolean}){const ref=useRef<HTMLTextAreaElement>(null); const submit=()=>{const value=ref.current?.value.trim();if(value&&!loading){onSend(value);if(ref.current)ref.current.value='';}}; return <div className="chat-input"><textarea ref={ref} autoFocus placeholder="Ask CafeAI anything..." rows={1} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();submit()}}}/><button onClick={submit} disabled={loading} aria-label="Send message"><Send size={18}/></button></div>}
