import { Coffee } from 'lucide-react'; import type { Message } from '../types/chat'; import { MessageBubble } from './MessageBubble'; import { TypingIndicator } from './TypingIndicator';
export function ChatMessages({messages,loading}:{messages:Message[];loading:boolean}){return <main className="chat-messages">{messages.length===0&&<div className="empty"><Coffee size={34}/><h3>Welcome to CafeAI</h3><p>What would you like to know?</p><div><span>Menu</span><span>Prices</span><span>Drinks</span><span>Food</span><span>Ingredients</span><span>Cafe details</span><span>Recommendations</span></div></div>}{messages.map((m,i)=><MessageBubble message={m} key={i}/>)}{loading&&<TypingIndicator/>}</main>}
